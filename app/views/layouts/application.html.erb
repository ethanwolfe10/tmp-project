<!DOCTYPE html>
<html>
  <head>
    <title>RailsFinalProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="wrapper">
        <% if user_signed_in? %>
          <h1 style="border-bottom: 0px;"> Welcome, <%= current_user.username %>! </h1>
          <br>
          <%= render 'layouts/navbar' %>
        <% else %>
          <h1> Welcome! </h1>
        <% end %>


        <%= yield %>
    </div>
  </body>
</html>


