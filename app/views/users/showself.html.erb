<%= render partial: 'profile', locals: {user_profile: current_user} %>

<br>

<% if !invites?(current_user).empty? %>
    <%= render partial: 'subscriptions/invite_index', collection: invites?(current_user), as: :sub %>
<% end %>