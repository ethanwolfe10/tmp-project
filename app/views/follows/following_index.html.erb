<% if !@followings.blank? %>
    <%= render partial: 'users/user_details', collection: @followings, as: :user_profile %>
<% else %>
    Here's Users to Follow: 

    <%= render partial: 'users/user_details', collection: unfollowed_users(current_user), as: :user_profile %>
<% end %>